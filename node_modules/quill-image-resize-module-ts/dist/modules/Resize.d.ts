import { ImageResize } from '../ImageResize';
import { BaseModule } from './BaseModule';
/**
 * Enables resize of image by dragging the corners of an image
 *
 * @export
 * @class Resize
 * @extends {BaseModule}
 */
export declare class Resize extends BaseModule {
    private boxes;
    private listeners;
    private resizeBox;
    private clickPosition;
    private originalWidth;
    constructor(resize: ImageResize);
    private onMouseDown();
    private onMouseDrag();
    private onMouseRelease();
    private createBox(cursorType);
    private positionBoxes();
    onCreate(): void;
    onDestroy(): void;
    onUpdate(): void;
    private setCursorStyle(cursorStyle);
}
